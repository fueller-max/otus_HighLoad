#SPDX-License-Identifier: MIT-0
---
# tasks file for consul-client
- name: Install Consul client
  tags: install
  ansible.builtin.include_tasks: 
    file: install.yaml
  when: ansible_facts["os_family"] == "Debian"

- name: Provision Consul client
  tags: provision
  ansible.builtin.include_tasks: 
   file: provision.yaml
  when: ansible_facts["os_family"] == "Debian"

- name: Provision Consul client. Systemd part
  tags: provision
  ansible.builtin.include_tasks: 
   file: provision_systemd.yaml
  when: ansible_facts["os_family"] == "Debian"  